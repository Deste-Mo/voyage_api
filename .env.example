# Copiez ce fichier en ".env" et remplacez les valeurs

# Port fourni automatiquement par Railway (ne pas définir dans Railway)
# PORT=3000

# Secret JWT
JWT_SECRET=change_me

# Chaîne de connexion PostgreSQL (Railway)
DATABASE_URL=postgres://USER:PASSWORD@HOST:5432/DB_NAME
# Ou en fallback si Railway fournit DATABASE_PUBLIC_URL
# DATABASE_PUBLIC_URL=postgres://USER:PASSWORD@HOST:5432/DB_NAME

# Variables PG alternatives (si vous préférez)
# PGHOST=localhost
# PGPORT=5432
# PGUSER=postgres
# PGPASSWORD=postgres
# PGDATABASE=travel_booking

# Twilio (optionnel)
# TWILIO_ACCOUNT_SID=
# TWILIO_AUTH_TOKEN=
# TWILIO_FROM_NUMBER=